services:
  - type: web
    name: shillong-teer-dashboard
    env: node
    plan: free
    rootDir: .          # must be root, not "New project"
    buildCommand: npm install
    startCommand: node server.js
    autoDeploy: true
    nodeVersion: 20
    healthCheckPath: /api/live
